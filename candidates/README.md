# Preserved Candidates

Archaeology candidates are preserved in the aggregate machine-readable ledgers under `inventory/`. They are **not** loaded by the runtime compiler.

Promotion requires an explicit Foundry decision and moves the reusable contract into `skills/`, `runtime-contracts/`, or `profiles/` while retaining provenance and the original candidate ID in inventory history.
