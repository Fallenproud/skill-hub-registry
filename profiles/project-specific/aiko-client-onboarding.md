# AIKO Client Onboarding — Source Profile

Provenance profile for historical candidate `SK-023`.

Reusable workflow extracted separately: business scan → package recommendation → compliance/document checks → signing → delivery → archive.

This profile retains AIKO-specific naming, package assumptions, document templates, and product behavior. Those details must not leak into the generic onboarding skill contract.
