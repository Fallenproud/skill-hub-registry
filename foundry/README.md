# Skill Foundry

The Foundry turns recovered expertise into governed skill packages.

`Archaeology → Source capture → Isolation → Normalization → Deduplication → Contracting → Validation → Testing → Packaging → Promotion`

The Foundry is deliberately separate from the runtime. Discovery can preserve incomplete candidates without exposing them as executable capabilities.
